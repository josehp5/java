public class PracticeProblems {
    public static double sumGrid(int[][] grid) {
        int sum = 0;

        for (int[] row : grid) {
            for (int value : row) {
                sum += value;
            }
        }

        return sum;
    }
  }
